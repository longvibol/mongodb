MongoDB : GE Command 

1. Create DB 

>> use studentDB

1.1 Show collection :

>> show collections

2. Check DB 

>> db.student.insertOne({name:"Vibol",age:34})

----------------------------------------

2.2 Assign const to replace : db prefix 

const myCollection = db.collectionName;

------------------------------------------

3. Create DB 

>>  db.carDB.insertOne({name:'RoseRoy',year:'2024'})

4. Show DB

>> carDB> db.carDB.find()

5. Show Database 

>> show dbs

6. Select database 
>> db.show 

admin             40.00 KiB
articledb         72.00 KiB
carDB            144.00 KiB
card_service_db   72.00 KiB
config           108.00 KiB
loan_service_db   72.00 KiB
local            192.00 KiB
studentDB        112.00 KiB

>> use studentDB

7. find Type :typeof

typeof db.companies.findOne().name

*** show collection name ***
>> db.getCollectionNames()


8. Check Validation Rules 

>> db.getCollectionInfos()

9. Deleted Validation Rules 
db.runCommand({ collMod: "posts", validator: {}, validationLevel: "off" })

*** 

10. Drop collection 

db.myCollection.drop() 

>> db.posts.drop()


11. Deleted field : 

>> db.hobbies.deleteOne({_id:"movie"})

studentDB> db.hobbies.find()
[
  { _id: 'sport', title: 'Sport' },
  { _id: 'cooking', title: 'Cookding' },
  { _id: 'car', title: 'car' }
]

=====================================================

Note In transaction : rollback : meaning : 

- if one of the condition error on the transaction it will rollback to do the first condition 
- rollback mean do from first condition 


--------------------------------------
































